{
  "name": "airline-bot-backend",
  "version": "1.0.0",
  "description": "Demo Airline Bot backend API - plain JavaScript, Supabase Postgres, deployed as Vercel serverless functions. No auth, all endpoints open, mock inventory/payment.",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
